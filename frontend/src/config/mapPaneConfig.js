export const MAP_PANE = Object.freeze({
  ROUTE: Object.freeze({
    name: 'route-pane',
    zIndex: 420,
  }),
  CREATURE: Object.freeze({
    name: 'creature-marker-pane',
    zIndex: 640,
  }),
  PLAYER: Object.freeze({
    name: 'player-marker-pane',
    zIndex: 660,
  }),
  SHARED_ROOM_CREATURE: Object.freeze({
    name: 'shared-room-creature-marker-pane',
    zIndex: 680,
  }),
})
